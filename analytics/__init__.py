"""
analytics — Portfolio calculation and analytics engines
========================================================
Core deterministic calculation modules. No LLM involvement.

Modules:
    calculate_xirr       — XIRR, FIFO cost basis, portfolio performance
    calc_allocations     — Asset sub-class categorisation (look-through)
    benchmark            — Nifty 50 benchmark cache
    mf_data_fetcher      — MF NAV, CAGR, and peer data fetcher
    peer_returns_engine  — Peer group risk metrics (Sharpe, Alpha, Beta)
    fetch_allocations    — AMC allocation scraping (Equity/Debt/Gold split)
"""
